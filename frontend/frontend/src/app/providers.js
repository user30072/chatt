'use client';

// This is just a passthrough component now that the real Providers implementation
// is in frontend/src/components/Providers.jsx
export function Providers({ children }) {
  return <>{children}</>;
} 