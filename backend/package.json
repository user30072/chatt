{
  "name": "backend-runner",
  "private": true,
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "start": "cd backend && npm install --omit=dev && npx prisma db push && npm run deploy || true && npm start"
  }
}


